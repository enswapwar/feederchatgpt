# Render に「このアプリは Python で Flask だぞ」と教える設定
services:
  - type: web
    name: feeder-backend
    env: python
    plan: free
    region: singapore
    rootDir: .
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn feeder:app"
